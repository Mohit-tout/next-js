"use client";

import ProtectedRoute from "../protected-route";

const AdminDashboard = () => {
  return (<>
  <h1 className="text-2xl font-bold">Welcome to Admin Dashboard</h1>
  <p>Manage users, orders, and reports here.</p>
  </>
  );
};

export default AdminDashboard;
